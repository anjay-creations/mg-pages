{
  "name": "aigyaan",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m backend.server",
    "start": "python3 -m backend.server",
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests -p \"test_*.py\"",
    "jobs:collect": "python3 -m backend.pipeline",
    "jobs:watch": "python3 -m backend.pipeline --loop",
    "build": "python3 scripts/build_site.py"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "^3.53.0-build1"
  }
}
